import ExamInterface from "@/pages/ExamInterface";

// Wrapper to keep current implementation while routed under feature slice
export default function ExamInterfacePage() {
  return <ExamInterface />;
}
